"""
Dataset model definition(s)

Definition(s) because we may have more than one way of doing this in future.
For example, datasets backed by local files
compared to remote access datasets
(where the choice of local storage could be more flexible).
"""
